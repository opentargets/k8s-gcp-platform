# This is a list of what to do as 'next steps'

1. Test the commands in the infrastructure helper for POS images.
  a. Testing 'deploy_disks', it fails when producing the overlays.
2. Create a Pull Request with the changes to the overlays back to the corresponding branch in the repository.