# This is a list of what to do as 'next steps'

1. Refactor the code in the command router to use a dictionary instead of a series of if/elif/else statements.
2. Refactor out how image listing file paths are calculated.