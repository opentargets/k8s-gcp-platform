# This is a list of what to do as 'next steps'

1. Update helper() to run on all images for a combination of (product, environment) to PR the new kustomizations back to branch
2. Iterate over the terraform output given a product and environment to update the kustomizations