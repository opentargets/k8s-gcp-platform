apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    app: web
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
      tier: frontend
  template:
    metadata:
      labels:
        app: web
        tier: frontend
    spec:
      containers:
      - name: web
        #image: macbre/nginx-http3:1.23.4
        image: nginx:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: web-port