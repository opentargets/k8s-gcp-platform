apiVersion: v1
kind: Service
metadata:
  name: web-external
  labels:
    app: web
    tier: external
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: web-port
    name: web-port-svc-external
  selector:
    app: web
    tier: frontend
  type: NodePort